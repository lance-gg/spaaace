version: 0.2

phases:
  pre_build:
    commands:
      - echo NPM Install 
      - npm install
  build:
    commands:
      - echo Building
      - npm run build
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files: **/*
